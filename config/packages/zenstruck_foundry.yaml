when@dev: &dev
    zenstruck_foundry:
        persistence:
            flush_once: true
        make_factory:
            default_namespace: 'App\DataFixtures\Factory'
        make_story:
            default_namespace: 'App\DataFixtures\Story'
        faker:
            locale: fr_FR

when@test: *dev
